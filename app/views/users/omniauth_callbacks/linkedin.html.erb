<h2> Sign up via LinkedIn</h2>
<%= form_for(@user, :url =>{:action=>:create},:html=>{:id=>'register_new_user'}) do |f| %>
    <%= render 'form',f: f%>
    <%= f.hidden_field :linkedin_uid  %>
    <%= f.hidden_field :provider%>
    <div>
      <%= f.submit "Save & Continue" %>
    </div>
<%end%>
